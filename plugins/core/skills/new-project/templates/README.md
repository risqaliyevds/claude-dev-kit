# {{PROJECT_NAME}}

{{ONE_LINE_DESCRIPTION}}

## Getting started

```bash
# install deps, then run:
{{DEV_COMMAND}}
```

## Development

- Tests: `{{TEST_COMMAND}}`
- Lint: `{{LINT_COMMAND}}`
- Build: `{{BUILD_COMMAND}}`

See `CLAUDE.md` for project conventions and `CHANGELOG.md` for history.
